# MF Portfolio Configuration
# Copy this to .env and customize if needed

# No environment variables required for basic operation
# The app uses a bundled SQLite database (public/data/mf_portfolio.db)
